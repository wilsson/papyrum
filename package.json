{
  "name": "root",
  "private": true,
  "scripts": {
    "bs": "lerna bootstrap && lerna run dev --stream"
  },
  "devDependencies": {
    "lerna": "^3.11.1",
    "typescript": "3.3.3"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
